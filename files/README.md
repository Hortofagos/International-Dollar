# Runtime Files

This directory holds local wallet/node state created on first run. Runtime state, passphrase handoff data, public IP cache, transport keys, and temporary wallet-generation data are stored as JSON and ignored by git.

Do not commit real runtime files from this directory.
